apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-team-quota
  namespace: dev
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: "10Gi"
    limits.memory: "15Gi"
    pods: "20"
    services: "10"