- name: notify and handler playbook
  hosts: all
  become: yes
  tasks:
    - name: installation of apache
      yum: name=httpd state=present
      notify: start apache
  handlers:
    - name: start apache
      service: name=httpd state=started