apiVersion: apps/v1
kind: Deployment
metadata:
  name: zomato-deployment
  labels:
    app: food
  annotations:
    kubernetes.io/change-cause: "version1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: food #this should match with pod labels
  template:
    metadata:
      labels: 
        app: food
    spec:
      containers:
        - name: zomato-container
          image: kastrov/zomato
---
apiVersion: v1
kind: Service
metadata:
  name: zomato-service
spec:
  type: LoadBalancer
  selector:
    app: food
  ports:
    - port: 80
      targetPort: 3000