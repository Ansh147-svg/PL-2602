- name: printing the worker nodes info
  hosts: all
  tasks:
    - name: print the node info
      debug:
        msg: "server name is: {{ ansible_nodename }}, number of CPUs: {{ ansible_processor_vcpus }}, total memory size: {{ ansible_memtotal_mb }}, the OS family is: {{ ansible_os_family }}"
        